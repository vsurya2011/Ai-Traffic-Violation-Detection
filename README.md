# AI-Powered Traffic Violation Detection Web App
This app uses YOLOv8m to detect traffic violations from live camera input.
Built with Flask + Ultralytics + Render deployment.

## Run locally
```bash
pip install -r requirements.txt
python app.py
